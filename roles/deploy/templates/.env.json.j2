{
        "access_token": "{{ lookup('passwordstore', 'dev/mfa/gh-access-token') | regex_replace('\"', '\\\"') }}",
        "webhook_secret": "{{ lookup('passwordstore', 'dev/mfa/gh-webhook-secret') | regex_replace('\"', '\\\"') }}",
        "output_dir": "/var/github"
}
